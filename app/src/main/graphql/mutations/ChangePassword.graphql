mutation ChangePasswordMutation($newPassword: String!, $newPasswordConfirm: String!) {
    changePassword(newPassword: $newPassword, newPasswordConfirm: $newPasswordConfirm) {
        success
        message
    }
}