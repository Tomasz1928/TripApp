# ============================================================
# Plik: app/src/main/graphql/com/example/tripapp2/queries/TripList.graphql
# ============================================================
query TripListQuery {
    tripList {
        trips {
            id
        }
    }
}
