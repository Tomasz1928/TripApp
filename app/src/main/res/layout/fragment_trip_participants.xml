<?xml version="1.0" encoding="utf-8"?>
<!--
    Trip Participants Fragment Layout
    Struktura: Obrazek (25%) → Nagłówek → Filtry (dla właściciela) → Treść (Lista uczestników)
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- ================================== -->
        <!-- HEADER IMAGE - 25% wysokości ekranu -->
        <!-- ================================== -->
        <FrameLayout
            android:id="@+id/headerImageContainer"
            style="@style/HeaderImageContainer"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHeight_percent="0.25">

            <ImageView
                android:id="@+id/headerImage"
                style="@style/HeaderImage"
                android:src="@drawable/header_l_participants"/>
        </FrameLayout>

        <!-- ================================== -->
        <!-- NAGŁÓWEK WIDOKU (WYŚRODKOWANY) -->
        <!-- ================================== -->
        <TextView
            android:id="@+id/participantsTitle"
            style="@style/Text.Heading.Screen"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/nav_participants"
            android:gravity="center"
            android:textAlignment="center"
            app:layout_constraintTop_toBottomOf="@id/headerImageContainer"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- ================================== -->
        <!-- FILTRY/AKCJE - Przewijalne poziomo (TYLKO DLA WŁAŚCICIELA) -->
        <!-- ================================== -->
        <HorizontalScrollView
            android:id="@+id/actionsScroll"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            android:paddingTop="@dimen/spacing_small"
            android:paddingBottom="@dimen/spacing_small"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/participantsTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <LinearLayout
                android:id="@+id/actionsContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="@dimen/padding_default"
                android:paddingEnd="@dimen/padding_default">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/actionAll"
                    style="@style/Button.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/participants_action_all"/>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/actionAdd"
                    style="@style/Button.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/participants_action_add"
                    android:layout_marginStart="@dimen/spacing_tiny"/>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/actionDetach"
                    style="@style/Button.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/participants_action_detach"
                    android:layout_marginStart="@dimen/spacing_tiny"/>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/actionDelete"
                    style="@style/Button.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/participants_action_delete"
                    android:layout_marginStart="@dimen/spacing_tiny"/>
            </LinearLayout>
        </HorizontalScrollView>

        <!-- ================================== -->
        <!-- TREŚĆ - Lista uczestników -->
        <!-- ================================== -->
        <ScrollView
            android:id="@+id/scrollParticipants"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:fillViewport="true"
            android:clipToPadding="false"
            app:layout_constraintTop_toBottomOf="@id/actionsScroll"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <LinearLayout
                android:id="@+id/participantsContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="@dimen/padding_default"
                android:paddingEnd="@dimen/padding_default"
                android:paddingTop="@dimen/spacing_medium"
                android:paddingBottom="@dimen/bottom_nav_padding"/>
        </ScrollView>

        <!-- Empty State -->
        <LinearLayout
            android:id="@+id/emptyState"
            style="@style/EmptyState"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:id="@+id/emptyStateTitle"
                style="@style/Text.Body.Bold"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/participants_empty_state_title"
                android:textColor="@color/text_secondary"/>

            <TextView
                android:id="@+id/emptyStateMessage"
                style="@style/Text.Caption"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_small"
                android:text="@string/participants_empty_state_message"
                android:textAlignment="center"/>
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>