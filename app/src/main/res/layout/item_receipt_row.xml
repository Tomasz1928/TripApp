<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="@dimen/spacing_small"
    android:gravity="center_vertical">

    <!-- Label -->
    <TextView
        style="@style/Text.Caption"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:text="@string/receipt_label"
        android:textColor="@color/text_hint"/>

    <!-- Content area -->
    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1">

        <!-- Stan 1: Dodaj rachunek -->
        <TextView
            android:id="@+id/receiptAddButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/receipt_add"
            android:textSize="12sp"
            android:textColor="@color/primary"
            android:background="@drawable/bg_receipt_button_add"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            android:clickable="true"
            android:focusable="true"
            android:visibility="gone"
            tools:visibility="visible"/>

        <!-- Stan 2: Zobacz rachunek + hint -->
        <LinearLayout
            android:id="@+id/receiptViewContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView
                android:id="@+id/receiptViewButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/receipt_view"
                android:textSize="12sp"
                android:textColor="@color/success"
                android:background="@drawable/bg_receipt_button_view"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:clickable="true"
                android:focusable="true"/>

            <TextView
                android:id="@+id/receiptLongPressHint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/receipt_long_press_hint"
                android:textSize="9sp"
                android:textColor="@color/text_hint"
                android:fontFamily="@font/plus_jakarta_sans"
                android:layout_marginTop="2dp"/>
        </LinearLayout>

        <!-- Stan 3: Ikony kwadratowe 36x36 -->
        <LinearLayout
            android:id="@+id/receiptActionsContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:visibility="gone">

            <!-- Zobacz -->
            <FrameLayout
                android:id="@+id/receiptViewButtonSmall"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_receipt_icon_view"
                android:clickable="true"
                android:focusable="true"
                android:contentDescription="@string/receipt_view_short">

                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_receipt_view"
                    app:tint="@color/success"
                    xmlns:app="http://schemas.android.com/apk/res-auto"/>
            </FrameLayout>

            <!-- Zmień -->
            <FrameLayout
                android:id="@+id/receiptChangeButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_receipt_icon_change"
                android:clickable="true"
                android:focusable="true"
                android:contentDescription="@string/receipt_change">

                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_receipt_change"
                    app:tint="@color/warning"
                    xmlns:app="http://schemas.android.com/apk/res-auto"/>
            </FrameLayout>

            <!-- Usuń -->
            <FrameLayout
                android:id="@+id/receiptDeleteButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_receipt_icon_delete"
                android:clickable="true"
                android:focusable="true"
                android:contentDescription="@string/receipt_delete">

                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_receipt_delete"
                    app:tint="@color/error"
                    xmlns:app="http://schemas.android.com/apk/res-auto"/>
            </FrameLayout>
        </LinearLayout>

        <!-- Loading -->
        <ProgressBar
            android:id="@+id/receiptProgress"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:visibility="gone"/>

    </FrameLayout>
</LinearLayout>