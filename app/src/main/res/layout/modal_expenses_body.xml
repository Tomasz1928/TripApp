<?xml version="1.0" encoding="utf-8"?>
<!--
    Expenses Body v2 — Propozycja C
    Zmiany: total jako hero kwota, separator delikatniejszy, scroll zachowany
    Wszystkie ID ZACHOWANE
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/modalBodyRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/padding_default">

    <!-- Total Cost (hero style) -->
    <TextView
        android:id="@+id/totalCostText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_size_title"
        android:textStyle="bold"
        android:textColor="@color/primary"
        android:text="Suma: 0,00 PLN"
        android:paddingBottom="@dimen/spacing_small"/>

    <!-- Separator -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/divider"
        android:layout_marginBottom="@dimen/spacing_small"/>

    <!-- Scrollable currency list -->
    <ScrollView
        android:id="@+id/scrollCosts"
        android:layout_width="match_parent"
        android:layout_height="150dp">

        <LinearLayout
            android:id="@+id/expensesListContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:divider="@drawable/divider_horizontal"
            android:showDividers="middle"/>
    </ScrollView>
</LinearLayout>
