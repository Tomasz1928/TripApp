<?xml version="1.0" encoding="utf-8"?>
<!--
    Item Settlement Detail Cost
    Wiersz kosztu w tab "Koszty": tytuł + kwota z walutą + ikona rozliczenia (✓/✗)
    Używa istniejących styli
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="@dimen/padding_small"
    android:paddingHorizontal="@dimen/padding_tiny">

    <!-- Tytuł kosztu (lewa strona, rozciąga się) -->
    <TextView
        android:id="@+id/costTitle"
        style="@style/Text.Body"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Nocleg"
        android:ellipsize="end"
        android:maxLines="1"/>

    <!-- Kwota z walutą -->
    <TextView
        android:id="@+id/costAmount"
        style="@style/Text.Body.Bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="150,00 PLN"
        android:layout_marginEnd="@dimen/spacing_small"/>

    <!-- Ikona rozliczenia (✓ zielony / ✗ czerwony) -->
    <ImageView
        android:id="@+id/settlementStatusIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_cross"
        app:tint="@color/error"
        android:contentDescription="@string/settlement_details_status_icon"/>

</LinearLayout>