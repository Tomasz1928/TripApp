<?xml version="1.0" encoding="utf-8"?>
<!--
    Item Settle Cost - Wiersz kosztu w tabie "Per koszty"
    ZMIENIONE: dual-currency — dodano costAmountSecondary (pomarańczowy)

    Kompaktowy: CheckBox | Tytuł (ellipsize) | Kwoty (primary + secondary)
    Kolory: zielony = ja płaciłem, czerwony = participant płacił
    Secondary (pomarańczowy) = kwota w walucie tripu, widoczny tylko gdy multi-currency
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="6dp"
    android:paddingHorizontal="@dimen/padding_small"
    android:background="?attr/selectableItemBackground"
    android:minHeight="40dp">

    <CheckBox
        android:id="@+id/costCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:paddingStart="0dp"/>

    <!-- Tytuł kosztu - ucinany z ... -->
    <TextView
        android:id="@+id/costTitle"
        style="@style/Text.Body"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_small"
        android:ellipsize="end"
        android:maxLines="1"
        android:singleLine="true"
        tools:text="Hotel Berlin na 3 noce"/>

    <!-- Kwoty (vertical: primary + optional secondary) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="end"
        android:layout_marginStart="@dimen/spacing_small">

        <!-- Kwota główna (primary, always visible) -->
        <TextView
            android:id="@+id/costAmount"
            style="@style/Text.Body.Bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="225,00 EUR"/>

        <!-- Kwota drugorzędna (secondary, visible only when multi-currency) -->
        <TextView
            android:id="@+id/costAmountSecondary"
            style="@style/Text.Caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textColor="@color/secondary"
            android:layout_marginTop="1dp"
            android:visibility="gone"
            tools:text="950,00 PLN"/>
    </LinearLayout>

</LinearLayout>